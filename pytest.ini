[pytest]
# 必须限定 testpaths。否则 pytest 会按默认规则去收集 app/test_cases.py
# —— 那是 156 条测试用例**名称**的常量表,不是测试代码,导入即报错。
testpaths = tests
python_files = test_*.py
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
